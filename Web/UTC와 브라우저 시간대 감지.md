### 📌 주제

**UTC(Coordinated Universal Time)** — 전 세계가 공통으로 사용하는 표준 시간

+

**브라우저에서 시간대를 감지하고 변환하는 방법**

---

### 🧩 핵심 개념

`UTC`는 **지구 전체의 기준 시간**이다.

각 나라의 현지 시간(Local Time)은 `UTC`에 오프셋(offset)을 더하거나 빼서 계산된다.

> 예:
> 
> - 한국 KST = UTC + 9
> - 미국 EST = UTC - 5

---

### 🕐 예시

| 지역 | 시간대 | 시각(예시) |
| --- | --- | --- |
| **UTC (기준)** | +00:00 | 2025-10-31 15:00 |
| **한국 (KST)** | +09:00 | 2025-10-31 24:00 (= 11월 1일 00:00) |
| **뉴욕 (EST)** | -05:00 | 2025-10-31 10:00 |
| **파리 (CET)** | +01:00 | 2025-10-31 16:00 |

---

### ⚙️ 왜 UTC를 사용하는가?

글로벌 서비스에서는 사용자마다 시간대가 다르기 때문에,

서버는 **UTC 기준으로 시간 데이터를 저장**해야 한다.

예를 들어 DayDot 앱처럼 여러 나라에서 사용될 때:

- 한국 사용자는 `2025-10-31 23:00 KST`에 일기를 씀
- 미국 사용자는 같은 날 `2025-10-31 10:00 EST`에 씀

→ 서버는 둘 다 **UTC 기준**으로 저장해야 “오늘 작성한 일기” 판단이 가능하다.

---

### 🧠 서버와 프론트엔드의 역할

| 구분 | 역할 |
| --- | --- |
| **서버 (Server)** | UTC 기준으로 모든 시간 저장 (`2025-10-31T14:00:00Z`) |
| **프론트엔드 (Client)** | 브라우저의 지역 시간대를 감지해서 현지 시간으로 표시 |

```tsx
// 서버에서 받은 UTC 시간
const utcString = "2025-10-31T14:00:00Z";

// 한국 시간(Asia/Seoul)으로 변환
const kstTime = new Date(utcString).toLocaleString("ko-KR", {
  timeZone: "Asia/Seoul",
});
console.log(kstTime); // "2025. 10. 31. 오후 11:00"

```

---

### 🌍 브라우저에서 시간대 감지하기

브라우저는 사용자의 로컬 시간대 정보를 **자동으로 제공**한다.

### ✅ 1. 감지하기

```tsx
const timezone = Intl.DateTimeFormat().resolvedOptions().timeZone;
console.log(timezone); // 예: "Asia/Seoul", "America/New_York"

```

### ✅ 2. UTC 기준 시간으로 변환

```tsx
const now = new Date();
console.log(now.toISOString()); // UTC 기준 ISO 문자열 (예: 2025-10-31T14:00:00.000Z)

```

### ✅ 3. 로컬 시간으로 다시 변환

```tsx
const utcString = "2025-10-31T14:00:00Z";
const localTime = new Date(utcString).toLocaleString(undefined, {
  timeZone: Intl.DateTimeFormat().resolvedOptions().timeZone,
});
console.log(localTime); // 브라우저 설정된 현지 시각

```

---

### 📦 용어 정리

| 용어 | 의미 | 예시 |
| --- | --- | --- |
| **UTC** | 세계 표준 시간 | `2025-10-31T14:00:00Z` |
| **KST** | 한국 표준시 (UTC+9) | `2025-10-31 23:00` |
| **EST** | 미국 동부 시간 (UTC-5) | `2025-10-31 09:00` |
| **Z (Zulu)** | UTC 기준임을 의미하는 접미사 | `"2025-10-31T14:00:00Z"` |
| **timeZone** | 브라우저 감지된 사용자 지역 | `"Asia/Seoul"`, `"Europe/Paris"` |

---

### 🧭 요약

> UTC는 전 세계의 기준 시간이며,
> 
> 
> 서버는 UTC로 저장하고
> 
> 클라이언트는 브라우저의 `timeZone` 정보를 사용해 **로컬 시간으로 변환**한다.
>
