## 🧩 1. `Exclude<Type, ExcludedUnion>`

> ExcludedUnion에 속하는 모든 유니언 멤버를 Type에서 제외한 타입을 생성한다.
> 

즉, `T` 타입에서 `U` 타입에 해당하는 멤버를 **빼는(subtract)** 역할을 한다.

> 💡 기억법: Exclude = 빼기
> 

### 📘 문법

```tsx
Exclude<Type, ExcludedUnion>

```

### 📙 예시

```tsx
type T0 = Exclude<"a" | "b" | "c", "a">;
// => "b" | "c"

type T1 = Exclude<"a" | "b" | "c", "a" | "b">;
// => "c"

type T2 = Exclude<string | number | (() => void), Function>;
// => string | number

```

### 🧠 작동 원리

```tsx
type Exclude<T, U> = T extends U ? never : T;

```

- `T`의 각 멤버가 `U`에 속한다면 제외(`never`)
- 아니면 유지

즉, `Exclude<'a' | 'b', 'a'>`는

`'a' extends 'a' ? never : 'a' → never`

`'b' extends 'a' ? never : 'b' → 'b'`

결과적으로 `'b'`가 남는다.

---

## 🧩 2. `Extract<Type, Union>`

> Union에 속하는 멤버만 Type에서 추출해온 타입을 생성한다.
> 

즉, `T` 타입에서 `U` 타입에 해당하는 멤버만 **남기는(intersect)** 역할을 한다.

> 💡 기억법: Extract = 교집합
> 

### 📘 문법

```tsx
Extract<Type, Union>

```

### 📙 예시

```tsx
type T0 = Extract<"a" | "b" | "c", "a" | "f">;
// => "a"

type T1 = Extract<string | number | (() => void), Function>;
// => () => void

```

### 🧠 작동 원리

```tsx
type Extract<T, U> = T extends U ? T : never;

```

- `T`의 각 멤버가 `U`에 속한다면 유지
- 아니면 제거

---

## ⚖️ Exclude vs Extract 비교

| 구분 | 의미 | 역할 | 예시 | 결과 |
| --- | --- | --- | --- | --- |
| **Exclude<T, U>** | `T`에서 `U`를 **제외** | 차집합 | `Exclude<'a' | 'b' |
| **Extract<T, U>** | `T`에서 `U`를 **추출** | 교집합 | `Extract<'a' | 'b' |

---

## 💡 정리

- `Exclude` → **타입 제거** (빼기)
- `Extract` → **타입 추출** (남기기)
- 두 개는 **반대 개념**이며, 조건부 타입(`extends`) 기반으로 동작한다.
- 유니언 타입을 조합해 복잡한 타입을 필터링할 때 자주 사용한다.
